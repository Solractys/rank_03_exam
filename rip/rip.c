int find_parent(void)
{
  ///aksdflsakjdflkasjdflkasjdf
  find_parent()
  return (0);
}

int check_path(char *str, int len, char c)
{
  str[len] = c;
  return (1);
}
int check_paren(char *str)
{
  //checa os parenteses
  //e retorna 
}
str[i] = " ";

int backtracking("(()())")
{
  while (str[i] != 0)
  {
    if(check_path(something) == str[i])
      printf("kasjdfk");
    if (backtracking(str) == 0)
      return (0);
  }
  return (0);
}

